# Stack API/worker (Phương án B) — TÁCH khỏi requirements.txt (vốn cho Streamlit).
# Cài: ./.venv/Scripts/python.exe -m pip install -r requirements-api.txt

# ── Web layer ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9        # parse multipart/form-data (upload file)
pydantic>=2.7.0
httpx>=0.27.0                  # TestClient + (tương lai) async AI client

# ── Lõi tính toán (dùng bởi package core/) ──
pandas>=2.0.0,<3.0.0
numpy>=1.26.0,<2.0.0
scikit-learn>=1.4.0,<2.0.0
mlxtend>=0.23.0
scipy>=1.13.0,<2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
openpyxl>=3.1.0
xlrd>=2.0.1
requests>=2.31.0

# ── Queue/worker (BƯỚC 3) ──
rq>=1.16.0
redis>=5.0.0
fakeredis>=2.20.0             # dev/test: chạy job inline không cần Redis server
